import React from 'react';
import { shallow } from 'enzyme';
import renderer from 'react-test-renderer';
import DreamJobsContainer from './DreamJobsContainer';

describe('<DreamJobsContainer />', () => {
  test('should have a test', () => {
    
  });
});
